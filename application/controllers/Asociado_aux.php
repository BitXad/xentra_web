<?php
/* 
 * Generated by CRUDigniter v3.2 
 * www.crudigniter.com
 */
 
class Asociado_aux extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Asociado_aux_model');
    } 

    /*
     * Listing of asociado_aux
     */
    function index()
    {
        $data['asociado_aux'] = $this->Asociado_aux_model->get_all_asociado_aux();
        
        $data['_view'] = 'asociado_aux/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new asociado_aux
     */
    function add()
    {   
        if(isset($_POST) && count($_POST) > 0)     
        {   
            $params = array(
				'id_asoc' => $this->input->post('id_asoc'),
				'nombre_asoc' => $this->input->post('nombre_asoc'),
				'codigo' => $this->input->post('codigo'),
				'direccion' => $this->input->post('direccion'),
				'servicio' => $this->input->post('servicio'),
				'lectura_ant' => $this->input->post('lectura_ant'),
				'lectura_act' => $this->input->post('lectura_act'),
				'consumo' => $this->input->post('consumo'),
				'fecha' => $this->input->post('fecha'),
				'hora' => $this->input->post('hora'),
				'estado' => $this->input->post('estado'),
				'gestion' => $this->input->post('gestion'),
				'mes' => $this->input->post('mes'),
            );
            
            $asociado_aux_id = $this->Asociado_aux_model->add_asociado_aux($params);
            redirect('asociado_aux/index');
        }
        else
        {            
            $data['_view'] = 'asociado_aux/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a asociado_aux
     */
    function edit($num)
    {   
        // check if the asociado_aux exists before trying to edit it
        $data['asociado_aux'] = $this->Asociado_aux_model->get_asociado_aux($num);
        
        if(isset($data['asociado_aux']['num']))
        {
            if(isset($_POST) && count($_POST) > 0)     
            {   
                $params = array(
					'id_asoc' => $this->input->post('id_asoc'),
					'nombre_asoc' => $this->input->post('nombre_asoc'),
					'codigo' => $this->input->post('codigo'),
					'direccion' => $this->input->post('direccion'),
					'servicio' => $this->input->post('servicio'),
					'lectura_ant' => $this->input->post('lectura_ant'),
					'lectura_act' => $this->input->post('lectura_act'),
					'consumo' => $this->input->post('consumo'),
					'fecha' => $this->input->post('fecha'),
					'hora' => $this->input->post('hora'),
					'estado' => $this->input->post('estado'),
					'gestion' => $this->input->post('gestion'),
					'mes' => $this->input->post('mes'),
                );

                $this->Asociado_aux_model->update_asociado_aux($num,$params);            
                redirect('asociado_aux/index');
            }
            else
            {
                $data['_view'] = 'asociado_aux/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The asociado_aux you are trying to edit does not exist.');
    } 

    /*
     * Deleting asociado_aux
     */
    function remove($num)
    {
        $asociado_aux = $this->Asociado_aux_model->get_asociado_aux($num);

        // check if the asociado_aux exists before trying to delete it
        if(isset($asociado_aux['num']))
        {
            $this->Asociado_aux_model->delete_asociado_aux($num);
            redirect('asociado_aux/index');
        }
        else
            show_error('The asociado_aux you are trying to delete does not exist.');
    }
    
}
